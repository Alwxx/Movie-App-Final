{
  "name": "movie-app",
  "version": "1.0.0",
  "description": "Alwin's Movie app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon backend/src/index.js",
    "client": "npm run dev --prefix frontend -- --host",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
